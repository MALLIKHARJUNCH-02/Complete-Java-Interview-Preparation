# SQL Interview Practice Questions (Without Queries)

Use this checklist to write the SQL yourself. The expected output tells
you what your result should look like.

## Level 1: Basic SQL (1--20)

### 1. Display all employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 2. Display only employee names.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 3. Display unique departments.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 4. Find employees whose salary is greater than 50,000.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 5. Find employees whose salary is between 30,000 and 60,000.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 6. Find employees working in the IT department.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 7. Find employees whose name starts with 'A'.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 8. Find employees whose name ends with 'n'.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 9. Find employees whose name contains 'ar'.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 10. Find employees with NULL email.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 11. Find employees with NOT NULL email.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 12. Sort employees by salary (ascending).

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 13. Sort employees by salary (descending).

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 14. Display the first 10 employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 15. Display employees whose salary is not equal to 40,000.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 16. Display employees hired after 2023.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 17. Display employees hired before 2022.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 18. Find employees from Hyderabad.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 19. Find employees whose department is IT or HR.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 20. Find employees whose salary is NOT between 20,000 and 40,000.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

## Level 2: Aggregate Functions (21--35)

### 21. Count total employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 22. Find maximum salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 23. Find minimum salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 24. Find average salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 25. Find total salary paid.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 26. Count employees in each department.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 27. Find average salary in each department.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 28. Find maximum salary department-wise.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 29. Find minimum salary department-wise.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 30. Departments having more than 5 employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 31. Departments whose average salary exceeds 50,000.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 32. Count female employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 33. Count male employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 34. Find total salary by city.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 35. Find highest-paid employee in each department.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

## Level 3: GROUP BY & HAVING (36--45)

### 36. Number of employees in each city.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 37. Departments with at least 3 employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 38. Cities having more than 10 employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 39. Average salary greater than 60,000.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 40. Maximum salary in every city.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 41. Minimum salary in every department.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 42. Total salary department-wise.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 43. Count employees by gender.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 44. Departments with exactly 2 employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 45. Departments with salary expenditure above 10 lakhs.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

## Level 4: Joins (46--60)

### 46. Display employees with department names.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 47. Display employees and project names.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 48. List employees without projects.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 49. List projects without employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 50. Display employees working in HR.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 51. Display employees and manager names.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 52. Display all departments even if they have no employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 53. Employees assigned to multiple projects.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 54. Display customer orders.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 55. Find customers without orders.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 56. Display products with suppliers.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 57. Find suppliers without products.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 58. Join three tables.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 59. Join four tables.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 60. Explain the difference between INNER, LEFT, RIGHT, and FULL OUTER JOIN.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

## Level 5: Subqueries (61--70)

### 61. Find employees earning above average salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 62. Find second highest salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 63. Find third highest salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 64. Find employee with highest salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 65. Find employees in the department with highest average salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 66. Find departments having maximum employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 67. Find employees working in the same department as Malli.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 68. Find employees earning more than their manager.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 69. Find employees who are not managers.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 70. Find employees without departments.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

## Level 6: Window Functions (71--80)

### 71. Rank employees by salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 72. Dense rank employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 73. Assign row numbers.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 74. Calculate running total of salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 75. Calculate cumulative average salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 76. Find highest salary in each department.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 77. Find second highest salary department-wise.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 78. Show previous employee salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 79. Show next employee salary.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 80. Find first employee in each department.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

## Level 7: Transactions (81--85)

### 81. Demonstrate BEGIN and COMMIT.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 82. Demonstrate ROLLBACK.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 83. Demonstrate SAVEPOINT.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 84. Rollback to a SAVEPOINT.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 85. Demonstrate ACID properties.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

## Level 8: Set Operators (86--90)

### 86. Combine results using UNION.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 87. Combine results using UNION ALL.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 88. Find common records using INTERSECT.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 89. Find differences using EXCEPT.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 90. Explain UNION vs UNION ALL.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

## Level 9: Real-World SQL (91--100)

### 91. Top 5 highest-paid employees.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 92. Top 3 salaries from each department.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 93. Employees hired in the last 30 days.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 94. Find duplicate email records.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 95. Remove duplicate records.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 96. Find employees who never received a salary increment.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 97. Find inactive customers.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 98. Generate monthly sales report.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 99. Find best-selling product.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.

### 100. Generate department-wise payroll report.

**Expected Output:**

-   A result set matching the requested information.
-   Appropriate columns, filtered/grouped/sorted as required.
-   Correct number of rows based on the data.
